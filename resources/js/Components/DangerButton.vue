<template>
    <button
        class="inline-flex items-center justify-center rounded-md border border-[var(--destructive)] bg-[var(--destructive)] px-4 py-2 text-xs font-medium uppercase tracking-widest text-[var(--destructive-foreground)] focus:outline-none focus:ring-1 focus:ring-[var(--destructive)] disabled:opacity-25"
    >
        <slot />
    </button>
</template>

